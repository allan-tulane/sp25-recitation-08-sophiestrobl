# CMPS 2200 Recitation 08

## Answers

**Name:** Sophie Strobl


Place all written answers from `recitation-08.md` here for easier grading.



- **1b)**
The work is O((V + E) log V).

The span is O(log V * V)


- **2b)**

If the parent dictionary is `{'a': 's', 'b': 's', 'c': 'b', 'd': 'c'}` and the destination is `'d'`, the function will return `'sbc'`, which represents the path from the source to the destination (excluding the destination itself).

The time complexity is proportional to the number of nodes in the path, or **O(L)** where **L** is the path length.
